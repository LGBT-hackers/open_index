<!DOCTYPE html>
<html>
<head>
  <title>Open Index</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav>
    <div class="container">
      <ul>
        <li>
          <%= link_to "All Companies", companies_path %>
        </li>
      </ul>
    </div>
  </nav>

  <%= form_tag companies_path, method: 'get' do %>
    <% if params[:name] %>
      <%= label_tag :name, "You are searching for" %>
    <% else %>
      <%= label_tag :name, "Search for" %>
    <% end %>
    <%= text_field_tag(:name, params[:name], placeholder: "Company") %>
    <%= submit_tag "Search", :name => nil %>
  <% end %>

  <% if notice %>
    <p class="alert alert-success"><%= notice unless notice.blank? %></p>
  <% end %>
  <% if alert %>
    <p class="alert alert-danger"><%= alert unless notice.blank? %></p>
  <% end %>

  <%= yield %>

  </body>
</html>
